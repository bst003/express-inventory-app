extends ../layout

block content
    h1 #{title} 
    form(action=postUrl method="post")
        div(class="input-contain")
            label(for="name") Name 
                span(class="required") *
            input(id="name" name="name" type="text" value=(style && style.name ? style.name : "") minlength="3" maxlength="150" required)
        input(type="Submit" value="Submit")  
    if errors 
        each error in errors 
            p(class="error") Error on field: #{error.path}, #{error.msg}